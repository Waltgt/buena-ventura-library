tags:
  - Users
parameters:
  - name: X-Username
    in: header
    type: string
    required: true
    description: Usuario que realiza la petición (debe tener rol Gestor o Administrador)
  - name: user_name
    in: path
    type: string
    required: true
    description: Nombre del usuario a validar
responses:
  200:
    description: Usuario válido
    schema:
      type: object
      properties:
        message:
          type: string
          example: User is valid
  400:
    description: Rol no válido
  404:
    description: Usuario no encontrado